/**
 * Bud for example html.
 */

"use strict";

module.exports = {
    force: true,
    mode: '444',
    data: {
        examples: require('../../ci/configs/banner-example-config.json')
    }
};

if (!module.parent) {
    require('coz').render(__filename);
}