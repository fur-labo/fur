/**
 * Bud file for index.js
 */

"use strict";

var cozTmpl = require('coz-tmpl');

module.exports = cozTmpl.indexJsBud({
    dirname: __dirname,
    desc: 'Hash utility module',
    module: 'fur/lib/util/hash'
});

if (!module.parent) {
    reuqire('coz').render(__filename);
}