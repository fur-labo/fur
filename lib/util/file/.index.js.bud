/**
 * Bud file for index.js
 */

"use strict";

var coz = require('coz');

module.exports = coz.bud.factory.indexJsBud({
    dirname: __dirname,
    desc: 'File utility module',
    module: 'fur/lib/util/file'
});

if (!module.parent) {
    coz.render(__filename);
}