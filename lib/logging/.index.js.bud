/**
 * Bud file for index.js
 */

"use strict";

var cozTmpl = require('coz-tmpl');

module.exports = cozTmpl.indexJsBud({
    dirname: __dirname,
    desc: 'fur logging module',
    module: 'fur/lib/logging'
});

if (!module.parent) {
    require('coz').render(__filename);
}