/**
 * Bud file for index.js
 */

"use strict";

var coz = require('coz');

module.exports = coz.bud.factory.indexJsBud({
    dirname: __dirname,
    desc: 'fur compiling module',
    module: 'fur/lib/compiling'
});

if (!module.parent) {
    coz.render(__filename);
}