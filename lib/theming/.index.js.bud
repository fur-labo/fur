/**
 * Bud file for index.js
 */

"use strict";

var coz = require('coz');

module.exports = coz.bud.factory.indexJsBud({
    dirname: __dirname,
    desc: 'fur theming module',
    module: 'fur/lib/theming'
});

if (!module.parent) {
    coz.render(__filename);
}